import { WidgetHeaderModule } from './widget-header.module';

/*
describe('WidgetHeaderModule', () => {
  let widgetHeaderModule: WidgetHeaderModule;

  beforeEach(() => {
    widgetHeaderModule = new WidgetHeaderModule();
  });

  it('should create an instance', () => {
    expect(widgetHeaderModule).toBeTruthy();
  });
});
*/
